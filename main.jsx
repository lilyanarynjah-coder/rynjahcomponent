import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.js' // IMPORTANT: Change this to './App.jsx' if your file ends in .jsx
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
